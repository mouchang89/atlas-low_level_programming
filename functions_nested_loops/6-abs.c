#include "main.h"

/**
 * _abs - Computes the absolute value of an integer.
 * @i: integer to be computed.
 *
 * Returns: Always 0.
 */
int _abs(int i)
{
	if (i >= 0)
		return (i);
	else
		return (-i);
}
