#include "main.h"

/**
 * main - Prints the alphabet in lowercase using only _putchar.
 * 
 * Return: Always 0.
 */
int main(void)
{
	char i = 'a';

	while (i <= 'z'
	{
		_putchar(i);
		i++;
	}
	_putchar('\n');

	return (0);
}
