#include "main.h"

/**
 * main - Prints the alphabet in lowercase using only _putchar.
 * 
 * Return: Always 0.
 */
int main(void)
{
	char i;

	for (i = 'a', c <= 'z'; i++)
	{
		_putchar(i);
	}
	_putchar('\n');
}
