#include "main.h"

/**
 * puts - Prints a string followed by new line to stdout.
 * @str: String to be printed.
 *
 * Return: Always 0.
 */
void _puts(char *str)
{
	char *str

	puts (str);

	_putchar('\n');
}
