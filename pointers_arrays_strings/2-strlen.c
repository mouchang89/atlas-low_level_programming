#include "main.h"
#include <string.h>

/**
 * _strlen - Returns length of a string.
 * @s: character variable.
 *
 * Return: Length of string.
 */
int _strlen(char *s)
{
	strlen(*s);

	return (*s);
}
