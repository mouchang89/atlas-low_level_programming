#include "main.h"

/**
 * _isdigit - checks for a digit 0 to 9.
 * @c: character to be checked.
 *
 * Return: 1 if c is a digit otherwise 0.
 */
int _isdigit(int c)
{
	int c <= 9

	if (c < 10)
		return (1);
	else
		return (0);
}
